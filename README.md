# Verifiable-Associative-Index
The Verifiable Associative Index (VAI) is a decentralized indexing protocol that replaces rigid routing with a Distributed Neural Network (DNN) to map Content IDs (CIDs) to file locations for faster lookup.
